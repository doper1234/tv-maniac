import { SubmitIfValidDirective } from './submit-if-valid.directive';

describe('SubmitIfValidDirective', () => {
  /*xit('should create an instance', () => {
    const directive = new SubmitIfValidDirective();
    expect(directive).toBeTruthy();
  });*/
});
